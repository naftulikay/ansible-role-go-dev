---
- hosts: localhost
  remote_user: root
  roles:
    # since this is the project name in git, we need this for travis
    - role: ansible-role-godev
      version: "{{ go_version }}"
      user_environment_file: "{{ go_env_file }}"
      global: false
      link_go_root: false
