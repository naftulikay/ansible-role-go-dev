---
# defaults file for godev
version: "1.7.4"
gopath: "{{ ansible_env.HOME }}"
global: true
link_go_root: true

user_environment_file: "{{ ansible_env.HOME }}/.bashrc"
user_install_base: "{{ ansible_env.HOME }}"
